type: handcrafted
obs_dim: 18             # = 6 + 2*n_lob_levels + 6, with n_lob_levels=3
features:
  # Market (6 + 2K features, K=n_lob_levels)
  - bid_ask_spread
  - midprice_log_return
  - queue_imbalance
  - signed_volume
  - realized_vol
  - rsi
  - lob_depth_bid_k_levels
  - lob_depth_ask_k_levels
  # Private (6 features)
  - inventory
  - active_bid_distance
  - active_ask_distance
  - outstanding_bid_offset
  - outstanding_ask_offset
  - time_remaining