# configs/reward/asymmetric.yaml
# Avellaneda-Stoikov flavour reward:
#   r = PnL - eta * max(0, inventory_pnl)
# Penalises adverse inventory PnL only.

reward_type: asymmetric
eta: 0.5      # inventory dampening — ablate: 0.1, 0.5, 1.0