# Vanilla DQN baseline agent, as ablation: distributional=False variant
type: dqn
hidden_dim: 256
lr: 0.0001
batch_size: 256
gamma: 0.99
target_update_freq: 1000
replay_buffer_size: 100000
epsilon_start: 1.0
epsilon_end: 0.05
epsilon_decay_steps: 50000