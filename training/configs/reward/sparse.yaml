# configs/reward/sparse.yaml
# Sparse diagnostic reward:
#   r = +1.0 if both sides filled (round-trip)
#   r = -0.5 if cross-spread fill (adverse selection)
#   r =  0.0 otherwise

reward_type: sparse